<!--
Thanks for contributing. Delete sections that genuinely do not apply — but
please do not delete the Deduplication section if you touched normalise/.
See CONTRIBUTING.md for the rules behind each checkbox.
-->

## What this changes

<!-- One or two sentences. What is different after this merges? -->

Closes #

## Why

<!-- The problem being solved. Link the issue, ADR, spec, or design section
     that motivates it. -->

## Which stage?

- [ ] **collect** — stage 1
- [ ] **normalise** — stage 2, fingerprinting and deduplication
- [ ] **score** — stage 3
- [ ] **publish** — stage 4
- [ ] **web** — triage interface
- [ ] **reports**
- [ ] CLI / scheduler / config / packaging
- [ ] Documentation only

---

## Checks

- [ ] `uv run ruff check src tests` passes
- [ ] `uv run ruff format --check src tests` passes
- [ ] `uv run pytest` passes
- [ ] The test suite still runs **without Docker and without any service**
- [ ] New comments cite the decision they implement (`ADR-000N`, `design §N.N`, `D12`)
- [ ] Commits follow Conventional Commits

## Architecture

- [ ] **The dependency direction is preserved**

  <!-- config ← everything; collect/ and normalise/ import nothing from db/;
       pipeline/ is the only layer that persists; web/ writes triage status
       only. If this PR changes that, it needs an ADR — say so below. -->

- [ ] No new `os.environ` read outside `config.py` <!-- add to Settings instead -->

## Deduplication

<!-- REQUIRED if this PR touches normalise/, fingerprinting, or employer/title/
     country handling. The asymmetry is the governing design constraint:
     a false merge CONCEALS a posting; a false split merely repeats one. -->

- [ ] Not applicable — this PR does not affect deduplication
- [ ] **I have asserted the non-merging cases, not only the merging cases**

  <!-- Prove that seniority and discipline variants stay DISTINCT, not only that
       cosmetic variants collapse. A PR with only merging assertions will be
       sent back for the separation cases. -->

- [ ] Any new `OBSERVED` fixtures are **real strings captured from a live board**,
      reproduced exactly — not tidied, not normalised, not retyped
- [ ] Validated against real captured output, not only handcrafted fixtures

## Migration

- [ ] No schema change in this PR
- [ ] **This PR includes a migration**, and:
  - [ ] I reviewed the autogenerated output by hand
  - [ ] Data migrations and any `CREATE EXTENSION` are present (autogenerate misses both)
  - [ ] New `NOT NULL` columns on populated tables use a `server_default`, dropped after backfill
  - [ ] Data is carried across before any column it lived in is dropped
  - [ ] `upgrade head` → `downgrade -1` → `upgrade head` verified locally
  - [ ] `alembic check` reports no new operations
  - [ ] **`down_revision` points at the current head on `main`**, not at a
        revision that exists only on another branch

## ADR

- [ ] Not needed — bug fix, test, docs, or a behaviour-preserving refactor
- [ ] **Needed, and included in this PR** — `Docs/ADRs/____`
- [ ] Needed, and accepted separately in #____
- [ ] Not sure — please advise

<!-- An ADR is needed when a change constrains future work: schema decisions,
     stage contracts, the dependency direction, or reversing an earlier ADR.
     ADRs are immutable once accepted — supersede, never edit. -->

## Spec

- [ ] Not applicable — small change, no spec
- [ ] Implements `specs/____/`, and the branch name matches that directory

---

## How to verify

<!-- What a reviewer should run or click to see this working. -->

```bash
cd app
```

## Notes for the reviewer

<!-- Anything deliberately left out, known limitations, or a decision you would
     like a second opinion on. -->
